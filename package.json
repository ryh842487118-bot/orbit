{
  "name": "orbit-universe-explorer",
  "version": "1.7.1",
  "private": true,
  "type": "module",
  "scripts": { "build": "node build.mjs", "start": "python3 -m http.server 8767 --bind 127.0.0.1", "test": "node --test tests/*.test.js" },
  "dependencies": { "three": "0.185.1" },
  "devDependencies": { "esbuild": "0.27.3" }
}
